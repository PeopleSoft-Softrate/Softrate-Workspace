# Shared fallback values for local development.
# Prefer service-specific .env files in each service folder.
MONGO_URI=mongodb://127.0.0.1:27017/softrate
JWT_SECRET=replace-with-a-strong-secret
FRONTEND_URL=http://localhost:4200
FROM_EMAIL=onboarding@resend.dev
RESEND_API_KEY=
